# Node
node_modules/
npm-debug.log
yarn-error.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/

# Capacitor — generated locally on the dev machine, not committed
www/
android/
ios/

# Firebase (push backend) — local/secret artifacts, not committed
functions/node_modules/
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
# FCM config: lives in android/app/ (already ignored); never commit it anywhere
google-services.json
GoogleService-Info.plist
*-firebase-adminsdk-*.json
